import React from 'react';
import ReactDOM from 'react-dom';
import Org from './Org';

ReactDOM.render(
    <React.StrictMode>
    <Org />
    </React.StrictMode>,
    document.getElementById('root')
);

