type config = {
    links: string[];
}

let Config: config = {
    links: [
    "/topics",
    "/search",
    "/today",
    "/write",
    //"/public/graph",
    "/public/kine",
    "/public/about"
]}

export default Config