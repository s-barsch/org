type config = {
    links: string[];
}

let Config: config = {
    links: [
    "/topics",
    "/search",
    "/write",
    "/today",
    //"/public/graph",
    "/public/kine",
    "/public/about"
]}

export default Config