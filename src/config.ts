type config = {
    links: string[];
}

let Config: config = {
    links: [
    "/topics",
    "/search",
    "/today",
    "/write",
    "/public/cache",
    "/public/about"
]}

export default Config