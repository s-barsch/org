type config = {
    links: string[];
}

let Config: config = {
    links: [
    "/search",
    "/write",
    "/today",
    "/public/graph",
    "/public/kine",
    "/public/about"
]}

export default Config